def train_fold(fold=0, epochs=20, batch_size=32, lr=1e-3,
               save_dir='/kaggle/working/', max_batches=None, device='cpu'):
    print(f"train_fold called — fold={fold}, epochs={epochs}")
    print("(not implemented yet)")